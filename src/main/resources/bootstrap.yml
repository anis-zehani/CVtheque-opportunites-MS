spring:
  application:
    name: Opportunites-MS
  cloud:
    config:
      discovery:
        enabled: true
        service-id: Config-MS

management:
  endpoints:
    web:
      exposure:
        include: ["health", "info", "refresh"]